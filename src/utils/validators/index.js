const userValidator = require('./user-validators');
const rentalValidator = require('./rental-validators');
const requestValidator = require('./request-validator');

module.exports = {
  userValidator,
  rentalValidator,
  requestValidator,
};
